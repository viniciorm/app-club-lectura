import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  basePath: '/app-club-lectura',
  // Si vas a usar imágenes con el basePath, asegúrate de actualizar las rutas si es necesario
};

export default nextConfig;
